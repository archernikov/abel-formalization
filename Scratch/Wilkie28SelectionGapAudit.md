# Wilkie 2.3/2.4 selection gap for Theorem 2.8

Source: Wilkie, *A theorem of the complement and some new o-minimal structures*, printed p. 403 (PDF p. 7) of `/Users/artemchernikov/Downloads/s000290050052 (1).pdf`. Section 2 explicitly says the properties in 2.3 and 2.4 are established in Maxwell [10], whose proofs Wilkie omits. This paper is evidence for the theorem statements, not a Lean proof or a task instruction.

For `C := charbonnelClosure (literalZeroSetFamily G)`, the maintained `literalZeroSet_charbonnelClosure_positiveArityOMinimalWeakSetStructure` supplies WS1–WS6 from geometricity, global smoothness, and UFF. `wilkie28WeakSelectionIncidence_sourceData` supplies the **entire algebraic input** to 2.3: `B := wilkie28WeakSelectionIncidence F f a` is a closed literal zero set in `C (1+n)`, `A := wilkie28FlatExceptionalSlice F f a` is in `C 1`, and `A` is exactly the first-coordinate projection of `B`. `wilkie28FlatExceptionalSlice_eq_exceptionalParameterSet` identifies `A` with the scalar singular-value set. These facts do **not** construct a selector.

The missing 2.3 theorem must turn `interior A ≠ ∅` and `A = projection B` into a **nonempty** open `U ⊆ A` and `φ : U → ℝⁿ` whose restricted graph is in `C (1+n)` and lies in `B`. Wilkie writes `U ∈ C 1` too. The maintained target is `Nonempty (Wilkie28WeakSelectedWitness C F f a)`: membership of each `(t,φ t)` in `B` yields `F (φ t)=a`, `f (φ t)=t`, and augmented-derivative nonsurjectivity via the residual-zero/minor-rank lemmas. The only extra adapters are the scalar/`RealEuclidean 1` homeomorphism, restricted-graph equality, and extension of `φ` outside `U` for the structure's total-function field. The source's n,m≥1 hypotheses hold in the intended 2.8 application (`n>k≥1`).

The separate missing 2.4 theorem must take an open family domain and a family-member graph of a function into `ℝⁿ`, then give a closed family-member `A_bad ⊆ ℝ` with empty interior on whose complement the function is `C¹`. The maintained `Wilkie28WeakSelectedWitness` stores `U_open` and `graph_mem` but omits `U_mem`; for this particular `C`, project the selected graph with `charbonnelClosure_projection` to derive `U_mem` before applying 2.4 (or add it to the generic source interface). Its `haeSmooth` premise retains exactly the weaker output needed here: `∀ t ∈ U \ A_bad, DifferentiableAt ℝ φ t`; it need not retain `A_bad ∈ C 1`. `exists_nonempty_open_differentiable_patch` and `wilkie28_smoothSingularWitnessSelection_of_weakSelection_and_aeSmooth` already finish remark 2.5 and feed the mathlib-only differential contradiction.

**Exact unsupported source theorem:** Maxwell's weak selection for o-minimal **weak set families**, followed by Maxwell's almost-everywhere `C^N` smoothness for functions whose **graphs** are in those families. The existing WS5 component bound, WS6 closed lift, projection closure, and the residual's smooth literal-zero presentation do not imply a family-member selector or `C¹` regularity of an arbitrary selected graph by any maintained/mathlib theorem found in this audit. General topological or measurable selection would also lack the required graph membership and smooth patch. A Lean/mathlib-only completion therefore needs proofs of the two family-level Maxwell theorems (or their specialized `hweak`/`haeSmooth` forms), not another algebraic incidence lemma; invoking the project's still-unproved o-minimality/cell-decomposition endpoint would be circular.
